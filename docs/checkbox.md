# 项目实现计划

本计划按“每一步都有可运行产物、验证命令、学习笔记输出”的方式推进。

已确定的实现选择：

- 后端 API 使用 Express + TypeScript。
- 自动测试使用 Vitest。
- 第一版按 API 先行推进，但核心算法仍拆成可测试的纯逻辑模块。
- 每完成一个主要阶段，都要把关键实现解释追加写入 `docs/learn.md`。
- 后端阶段同时保留 curl + 日志验证和自动测试。

## 1. 建立最小可运行后端

- [x] 先检查本机的环境
- [x] 初始化 Node.js + TypeScript 项目。
- [x] 安装 Express、TypeScript、后端开发启动工具和 Vitest。
- [x] 建立 `src/server.ts`，负责启动 Express 服务。
- [x] 建立 `src/app.ts`，负责创建 Express app。
- [x] 建立 `src/config/constants.ts`，预留课程固定常量位置。
- [x] 实现 `GET /health`，返回服务状态。
- [x] 配置 `npm run dev`，确保能启动后端服务。
- [x] 配置 `npm test`，确保能执行最小测试。
- [x] 使用 curl 验证 `GET /health`。
- [x] 将 `package.json`、TypeScript、Express app 和 server 的关系解释追加写入 `docs/learn.md`。
- [x] 完成后勾选本阶段已完成任务。

## 2. 实现课程配置 API

- [x] 定义课程固定常量：指令总数 320。
- [x] 定义课程固定常量：每页指令数 10。
- [x] 定义课程固定常量：页面总数 32。
- [x] 定义课程固定常量：内存块数 4。
- [x] 实现 `GET /api/config`，返回课程固定配置。
- [x] 为配置接口补充后端请求日志。
- [x] 编写测试，验证配置值正确。
- [x] 使用 curl 验证 `GET /api/config`。
- [x] 将常量、模块导出、接口返回 JSON 的解释追加写入 `docs/learn.md`。
- [x] 完成后勾选本阶段已完成任务。

## 3. 实现地址转换逻辑

- [x] 定义指令号、页号、页内偏移、内存块号、物理地址相关 TypeScript 类型。
- [x] 实现“指令号转页号”的函数。
- [x] 实现“指令号转页内偏移”的函数。
- [x] 实现“内存块号 + 页内偏移转物理地址”的函数。
- [x] 为所有地址转换函数定义补充注释，说明函数作用和参数。
- [x] 编写测试，覆盖典型指令号。
- [x] 编写测试，覆盖边界指令号，例如 0、9、10、319。
- [x] 将分页地址转换公式的解释追加写入 `docs/learn.md`。
- [x] 完成后勾选本阶段已完成任务。

## 4. 实现访问序列生成 API

- [ ] 实现访问序列生成逻辑，生成 320 条指令访问记录。
- [ ] 每条访问记录包含执行序号、指令号、来源类型。
- [ ] 来源类型至少区分顺序执行、前地址跳转、后地址跳转。
- [ ] 支持可选 `seed` 参数，方便复现结果。
- [ ] 实现 `GET /api/instructions?seed=1`。
- [ ] 编写测试，验证访问序列长度为 320。
- [ ] 编写测试，验证所有指令号都在 `0..319`。
- [ ] 编写测试，验证同一个 seed 生成相同序列。
- [ ] 使用 curl 验证访问序列接口。
- [ ] 在日志中输出 seed、序列长度和前几条访问记录。
- [ ] 将随机数、seed、访问序列生成规则的解释追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 5. 实现模拟器基础流程

- [ ] 实现模拟器基础状态，包含内存块、当前步骤、缺页次数。
- [ ] 实现单步执行逻辑：读取指令、计算页号、判断命中或缺页。
- [ ] 每一步结果包含指令号、页号、页内偏移、是否缺页。
- [ ] 每一步结果包含当前内存块快照和当前缺页次数。
- [ ] 暂时只跑通 FIFO 骨架：内存未满时装入页面，内存满时进入 FIFO 替换。
- [ ] 实现 `GET /api/simulations?algorithm=fifo&seed=1` 的基础返回。
- [ ] 编写测试，验证初始内存为空。
- [ ] 编写测试，验证首次访问某页一定缺页。
- [ ] 编写测试，验证重复访问已在内存的页不缺页。
- [ ] 使用 curl 验证 FIFO 基础模拟接口。
- [ ] 将模拟器状态和单步执行的解释追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 6. 实现 FIFO 页面置换

- [ ] 完整实现 FIFO 页面置换算法。
- [ ] 内存未满时直接装入页面。
- [ ] 内存已满时替换最早进入内存的页面。
- [ ] 模拟结果记录被换出的页号。
- [ ] 模拟结果记录被装入的页号。
- [ ] 模拟结果记录替换发生在哪个内存块。
- [ ] 编写测试，验证 FIFO 替换顺序正确。
- [ ] 编写测试，验证命中不会改变 FIFO 队列顺序。
- [ ] 编写测试，验证完整模拟能生成 320 条步骤。
- [ ] 使用 curl 验证 `GET /api/simulations?algorithm=fifo&seed=1`。
- [ ] 将 FIFO 的数据结构和状态变化解释追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 7. 实现 LRU 页面置换

- [ ] 实现 LRU 页面置换算法。
- [ ] 命中页面时更新最近使用状态。
- [ ] 缺页且内存满时替换最久未使用页面。
- [ ] 保持和 FIFO 相同的模拟结果结构。
- [ ] 支持 `GET /api/simulations?algorithm=lru&seed=1`。
- [ ] 编写测试，验证命中会更新最近使用顺序。
- [ ] 编写测试，验证缺页时替换最久未使用页。
- [ ] 编写测试，验证同一访问序列下 FIFO 和 LRU 可以产生不同结果。
- [ ] 使用 curl 验证 LRU 模拟接口。
- [ ] 将 LRU 与 FIFO 的区别追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 8. 实现 CLOCK 页面置换

- [ ] 实现 CLOCK 页面置换算法。
- [ ] 为每个内存块维护访问位。
- [ ] 命中页面时把访问位置为 `1`。
- [ ] 缺页且内存满时，指针扫描访问位。
- [ ] 访问位为 `1` 时置为 `0` 并跳过。
- [ ] 访问位为 `0` 时替换该页。
- [ ] 支持 `GET /api/simulations?algorithm=clock&seed=1`。
- [ ] 编写测试，验证命中会设置访问位。
- [ ] 编写测试，验证指针会跳过访问位为 `1` 的页面。
- [ ] 编写测试，验证替换后指针位置正确推进。
- [ ] 使用 curl 验证 CLOCK 模拟接口。
- [ ] 将 CLOCK 为什么近似 LRU 的解释追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 9. 完善 API 返回结构和日志

- [ ] 统一模拟 API 返回使用的算法。
- [ ] 统一模拟 API 返回 seed。
- [ ] 统一模拟 API 返回课程配置。
- [ ] 统一模拟 API 返回访问序列。
- [ ] 统一模拟 API 返回每一步执行记录。
- [ ] 统一模拟 API 返回总缺页次数。
- [ ] 统一模拟 API 返回缺页率。
- [ ] 不支持的算法返回 400。
- [ ] 非法 seed 返回 400。
- [ ] 日志输出每次模拟开始。
- [ ] 日志输出每一步命中或缺页。
- [ ] 日志输出每次页面置换。
- [ ] 日志输出模拟结束后的缺页次数和缺页率。
- [ ] 编写测试，验证 API 参数错误。
- [ ] 编写测试，验证三种算法都能返回完整结果。
- [ ] 编写测试，验证缺页率计算正确。
- [ ] 将 API、状态码和日志的作用解释追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 10. 前端搭建和 API 接入

- [ ] 初始化 Vue + Element Plus 前端项目。
- [ ] 建立算法选择控件。
- [ ] 建立 seed 输入控件。
- [ ] 建立运行模拟按钮。
- [ ] 展示课程配置。
- [ ] 展示缺页次数。
- [ ] 展示缺页率。
- [ ] 接入 `/api/config`。
- [ ] 接入 `/api/simulations`。
- [ ] 验证前端展示结果和 curl 返回一致。
- [ ] 将前端启动方式和调用后端 API 的说明追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 11. 前端模拟过程展示

- [ ] 展示每一步模拟记录表格。
- [ ] 表格包含执行序号、指令号、页号、页内偏移。
- [ ] 表格包含是否缺页、物理地址、置换信息。
- [ ] 展示当前内存块状态。
- [ ] 支持切换 FIFO、LRU、CLOCK 后重新运行。
- [ ] 验证三种算法都能正常展示。
- [ ] 验证缺页次数和缺页率与后端结果一致。
- [ ] 完成后勾选本阶段已完成任务。

## 12. Electron 打包

- [ ] 引入 Electron。
- [ ] 配置 Electron 加载前端构建产物。
- [ ] 明确后端服务在 Electron 中的启动方式。
- [ ] 添加客户端打包命令。
- [ ] 验证打包后客户端能打开。
- [ ] 验证打包后客户端能运行三种算法模拟。
- [ ] 将 Electron 主进程、渲染进程和打包流程解释追加写入 `docs/learn.md`。
- [ ] 完成后勾选本阶段已完成任务。

## 13. 最终检查

- [ ] 检查所有函数定义是否都有注释。
- [ ] 检查 `README.md` 是否包含完整运行方式。
- [ ] 检查 `docs/learn.md` 是否记录关键学习内容。
- [ ] 检查 `docs/checkbox.md` 是否准确反映完成进度。
- [ ] 完成一次从安装依赖、启动后端、curl 检查、运行测试、启动前端到客户端打包的完整验证。
