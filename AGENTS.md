# paging-simulator 项目

## 项目目标

本项目是“请求调页存储管理方式模拟程序”，用于模拟一个作业在请求调页系统中的执行过程，帮助理解页面、页表、地址转换、缺页中断和页面置换算法。

设定如下：

- 作业共有 320 条指令，地址空间为 32 页。
- 每页可存放 10 条指令。
- 主存最多同时装入 4 个页面。
- 初始状态下所有页面均未调入内存。
- 访问指令命中内存时，需要显示物理地址并继续下一条指令。
- 访问指令未命中内存时，需要记录缺页次数、调入页面；若 4 个内存块已满，需要执行页面置换。
- 所有 320 条指令执行完成后，需要计算并显示缺页率。
- 页面置换算法需要支持 FIFO、CLOCK、LRU 三种，并允许切换。

## 指令访问序列

指令访问次序按课程文档要求生成，整体比例为：

- 50% 顺序执行。
- 25% 均匀分布在前地址部分。
- 25% 均匀分布在后地址部分。

推荐实现方式：

1. 在 0 到 319 中随机选择起始指令 `m`。
2. 顺序执行 `m + 1`。
3. 随机跳转到前地址部分 `0..m-1` 中的某条指令 `m1`。
4. 顺序执行 `m1 + 1`。
5. 随机跳转到后地址部分 `m1 + 2..319` 中的某条指令 `m2`。
6. 顺序执行 `m2 + 1`。
7. 重复“前地址跳转、顺序执行、后地址跳转、顺序执行”，直到生成并执行 320 条指令。

实现时可把课程固定数据定义为常量，例如指令数、每页指令数、页数、内存块数。

## 技术栈

项目计划采用：

- 前端：Vue + Element Plus。
- 后端：TypeScript + Node.js，HTTP API 使用 Express。
- 自动测试：Vitest。
- 客户端：后续使用 Electron 打包。

开发优先级：

1. 先实现后端 API，并把核心算法拆成可测试的纯逻辑模块。
2. 用 `curl` 检查后端接口和运行日志，同时用 Vitest 覆盖核心逻辑。
3. 再实现前端界面。
4. 最后处理 Electron 客户端打包。

已定实现约定：

- 第一版按 API 先行推进，但不要把算法逻辑写死在路由处理函数里。
- 后端默认端口使用 `3000`，除非后续代码或用户明确调整。
- 随机访问序列接口和模拟接口应支持可选 `seed` 参数，方便复现、调试和测试。
- 后端基础接口包括 `GET /health`、`GET /api/config`、`GET /api/instructions?seed=1`、`GET /api/simulations?algorithm=fifo&seed=1`。
- `algorithm` 参数支持 `fifo`、`lru`、`clock`。
- 后端阶段同时保留 `curl + 日志` 验证和 Vitest 自动测试，不要只做其中一种。

## 后端实现关注点

- 后端是用户重点学习的部分，代码应清晰、易读，优先使用直接明确的结构。
- 所有函数定义都必须带注释，说明函数作用和参数。
- 所有代码注释一律使用中文书写，包括函数注释、行内注释和关键逻辑说明。
- 关键状态变化应打日志，尤其是每一步指令访问、命中/缺页、装入页面、页面置换、物理地址、当前内存块状态、累计缺页次数。
- API 设计应便于用 `curl` 验证。由于本程序主要是模拟和查询，除切换算法等场景外，不应依赖大量 POST 请求。
- 页面置换算法应保持可测试、可替换，避免把 FIFO、CLOCK、LRU 的状态更新逻辑混在同一大段代码里。
- 路由层只负责参数解析、调用模拟逻辑、返回 JSON 和记录请求级日志；地址转换、访问序列生成、页面置换、缺页率计算应放在独立模块中。
- 对非法参数要返回清晰的 400 响应，例如不支持的算法名或非法 seed。

## 前端实现关注点

- 前端可由代理主要负责实现，用户不重点学习前端细节。
- 界面需要清晰展示模拟过程，包括当前访问指令、页号、页内偏移、物理地址、是否缺页、内存块状态、使用算法、缺页次数和缺页率。
- 应支持 FIFO、CLOCK、LRU 的切换。
- 使用 Vue + Element Plus 时，优先遵循项目已有结构和组件习惯；如果尚未建立结构，保持目录和组件职责简单。

## 文档维护约定

- `docs/course-spec.md`：课程要求来源，不要随意改写。
- `docs/tech-stack-spec.md`：技术栈约定来源，不要随意改写。
- `docs/personal-demand-spec.md`：用户个人学习目标和协作方式来源，不要随意改写。
- `docs/checkbox.md`：项目进度安排。后续实现功能时，应把项目拆成合理任务；完成对应任务后打勾标记。
- `docs/learn.md`：学习笔记。用户明确要求“把回答写入 docs/learn.md 末尾”时，必须追加到文件末尾，不要覆盖原有内容。
- 常见工作流是：完成 `docs/checkbox.md` 中的相应内容，并把实现解释追加写入 `docs/learn.md`。

## 协作原则

- 先读相关文档和现有代码，再动手实现。
- 保持改动聚焦，不做无关重构。
- 不要覆盖用户已有改动。
- 对用户想学习的后端 TypeScript/Node.js 部分，代码和说明要偏教学友好。
- 对课程模拟规则，优先保持可验证性和可解释性，避免为了界面效果牺牲算法清晰度。
